# Build artifacts
*.o
*.obj
*.exe
*~

# Binaries
wspy
cpu_info
proctree
gpu_smi
amd_smi
wspy-validate
wspy-ledger
wspy-store
wspy-summary
wspy-plot

# Backup files
*.bak

# Editor / IDE
# .vscode/ is checked in (settings.json, extensions.json) so Claude Code and
# Cline share config across contributors; ignore only the per-user files.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# Misc
.DS_Store
amd_sysfs
nvidia_nvml
test_proctree
test_wspy
test_validate
test_ledger
test_ibs
test_power
test_phase
test_store
test_summary
test_plot
test_affinity
libwspy_profiler.so
test_hip_init
test_hip_kernel
doc/.~lock.ROCm Profiler SDK C++ API.docx#

# Generated by scripts/gen_compile_commands.py; contains absolute paths
# specific to this checkout, so each contributor generates their own.
compile_commands.json

# Python bytecode cache (web/server.py)
__pycache__/
*.pyc

# web/server.py's default run output root -- generated per-launch artifacts,
# not source.
web/runs/

# wspy-queue's / web/server.py's default job queue directory (item 13) --
# generated pending/running/done/failed job files, not source.
web/jobs/
