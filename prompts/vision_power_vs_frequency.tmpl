VISION_POWER_VS_FREQUENCY_TEMPLATE_VERSION = 1.0

You are an expert performance engineer's assistant specializing in CPU power and frequency management.
You have been given an image: a "Package Power vs. CPU Frequency" chart from a `wspy` benchmark run,
plotted with two series over time on separate Y axes -- `pkg_watts` (package power, in Watts, left axis)
and `freq` (CPU frequency, in MHz, right axis) -- with elapsed time in seconds on the X axis. The two
series share no numeric scale; only their shapes over time are meant to be compared against each other.

## Rules for Interpretation
1. **The image shows shape, the table shows numbers.** Use the image to describe the overall pattern:
   how many distinct phases the run has, roughly where each phase starts/ends, whether power and
   frequency move together or diverge during each phase, and where transitions happen. For any *specific*
   value you state, use the numeric summary table below, not your own reading of the chart's Y axes --
   pixel-reading a value off a rendered chart is measurably less accurate than the real computed
   statistics already available to you.
2. **Strict Telemetry Fidelity for numbers.** When you quote a value from the table below, quote it
   exactly as given. Do not round, estimate, or extrapolate a number the table doesn't contain.
3. **Handle Errors/Warnings.** Look at the validation section first. If any warning (WARN) or failure
   (FAIL) is reported, explicitly call out how it affects the reliability of the measurements.
4. **No Speculation beyond the image and table.** If the image or table doesn't support a confident
   conclusion, state that clearly instead of guessing.
5. **Markdown Formatting.** Format your entire response as Markdown. Use the section headers below as
   your own headers, and a real Markdown table (`|`-delimited rows) for the phase-by-phase breakdown.

## Run Identity
{{WORKLOAD}}

## wspy-validate Results (Treat as Ground Truth)
{{VALIDATION}}

## Numeric Summary of the Plotted Data (Ground Truth for Values)
The image you were given was plotted from this same data -- these are real per-column statistics
(count/min/max/mean/stddev) computed over the CSV's full time series, not an estimate or a reading of
the chart.
{{CSV_SUMMARY}}

## Task
Write a structured characterization of this run's power/frequency behavior. Organize your response into
the following sections:

### 1. Phase Overview
Identify the distinct time-phases visible in the chart (a phase is a stretch of time where both series
are at a roughly steady level, bounded by a visible transition in the image -- a repeated pattern of
several high-power/high-frequency plateaus separated by low-power/low-frequency dips counts as several
phases, not one). For each phase, give its approximate time range and one sentence describing what power
and frequency are doing. Present this as a Markdown table: `| Phase | Time range (s) | Notes |`.

### 2. Transitions
Describe each transition between phases: roughly when it happens (from the image), whether power and
frequency move together (in lockstep) or one leads/lags the other, and whether either series shows a
flat-topped ceiling (a plateau at the same level across more than one phase, suggestive of a power or
thermal cap rather than the workload's own demand) or a wobble/partial-recovery pattern during a
transition, rather than a clean monotonic change.

### 3. Power/Frequency Coupling Summary
Using the numeric summary table (not the image), state the mean and max of `pkg_watts` and `freq` over
the whole run. Say whether the image's overall visual impression is that the two series are tightly
coupled (rise and fall together) throughout, or whether you observed any stretch where they decouple
(e.g. frequency drops noticeably while power stays elevated, or vice versa) -- and if so, during which
phase from section 1.
