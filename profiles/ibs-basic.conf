# ibs-basic - single pass: unfiltered AMD IBS (ibs_fetch+ibs_op) sample
# counts, one-second interval time series (AMD-only; wspy warns and
# produces no IBS columns elsewhere).
ibs --csv --interval 1 --ibs-basic --no-ipc
