# ibs-memory-deep - single pass: AMD IBS with l3missonly+ldlat filtering on
# ibs_op for memory-path analysis, one-second interval time series -- known
# to skew the effective sampling period, see the run's own output for skew
# annotations.
ibs --csv --interval 1 --ibs-memory-deep --no-ipc
